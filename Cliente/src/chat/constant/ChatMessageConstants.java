package chat.constant;

import java.io.File;

public class ChatMessageConstants {
	public static String PATH_DEFAULT = new File(System.getProperty("user.dir")).getParent() + "/arquivos";

	public static int LENGTH_NAME_USER_SELECTED = 5;
}
